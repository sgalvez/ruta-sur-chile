{
  "name": "ruta-sur-chile",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "tsc --noEmit && node scripts/validate.mjs && vite build && node scripts/service-worker.mjs",
    "preview": "vite preview --host 0.0.0.0",
    "refresh": "node scripts/refresh.mjs",
    "validate": "node scripts/validate.mjs",
    "test": "node --test tests/*.test.mjs",
    "test:e2e": "playwright test"
  },
  "dependencies": {"leaflet": "^1.9.4"},
  "devDependencies": {"@playwright/test": "^1.55.0", "@types/leaflet": "^1.9.20", "cheerio": "^1.1.2", "typescript": "^5.9.2", "vite": "^7.1.5"}
}
